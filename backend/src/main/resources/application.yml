spring:
  application:
    name: civicresolver-backend

  data:
    mongodb:
      uri: mongodb://localhost:27017/civicresolver
      auto-index-creation: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org:
      springframework:
        security: DEBUG
